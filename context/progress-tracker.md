# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Project dialogs & editor home

## Current Goal

- Replace the mock project list with real persistence (Prisma project records + `app/api` routes) behind the `ProjectDialogIntent` seam.

## Completed

- Design system (shadcn/ui, lucide-react, `cn()` helper) — see `context/feature-specs/01-design-system.md`. Installed Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea; verified via `tsc --noEmit` and `next build`; forced dark theme via `dark` class on `<html>` since the app has no light/dark toggle.
- Editor chrome — see `context/feature-specs/02-editor-chrome.md`. Added `components/editor/editor-navbar.tsx` (fixed-height top navbar, left/center/right sections, sidebar toggle button with `PanelLeftOpen`/`PanelLeftClose`) and `components/editor/project-sidebar.tsx` (fixed overlay below the navbar, slides in/out via translate-x, `isOpen`/`onClose` props, shadcn `Tabs` with My Projects/Shared empty states, full-width `New Project` button with `Plus` icon). Wired both into `app/page.tsx` with `useState` sidebar toggle so they're actually reachable. The dialog title/description/footer pattern from the spec is already satisfied by the existing `components/ui/dialog.tsx` from the design-system phase (uses `--popover`/`--muted` tokens) — no new dialog file was needed and no concrete dialogs were built, per spec. Verified via `tsc --noEmit`, `eslint`, `next build`, and a curl smoke test of the dev server's rendered HTML for the expected navbar/sidebar text.
- Auth — see `context/feature-specs/03-auth.md`. Installed `@clerk/ui`. `app/layout.tsx` wraps `<body>`'s children in `ClerkProvider` with `appearance` from the new `lib/clerk-appearance.ts` (Clerk's `dark` theme from `@clerk/ui/themes` as the base, `variables` overridden with `var(--primary)`, `var(--card)`, `var(--muted)`, `var(--input)`, `var(--border)`, `var(--ring)`, `var(--destructive)`, `var(--radius-2xl)`, `var(--font-geist-sans)` etc. — the app's real shadcn/Tailwind tokens from `globals.css`, no hardcoded colors). Added `components/auth/auth-layout.tsx`, a two-panel layout (left: logo/tagline/text-only feature list, hidden below `lg`; right: centered form) used by the new `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` (`<SignIn />`/`<SignUp />`). Added `proxy.ts` at the root (Next.js 16 renamed `middleware.ts` → `proxy.ts`) with `clerkMiddleware` + `createRouteMatcher`, protecting everything except `/`, `/sign-in(.*)`, `/sign-up(.*)`. Moved the editor-chrome page body from `app/page.tsx` to `app/editor/page.tsx`; `app/page.tsx` is now an async Server Component that calls `auth()` and redirects to `/editor` (authenticated) or `/sign-in` (unauthenticated). Added `<UserButton />` to `EditorNavbar`'s right section. Verified via `tsc --noEmit`, `eslint`, `next build` (route table confirms `proxy.ts` is picked up as the Proxy/Middleware layer), and curl smoke tests against the dev server: `GET /` → 307 to `/sign-in`, `GET /editor` (unauthenticated) → 307 to `/sign-in?redirect_url=...`, `GET /sign-in` → 200 with the auth layout's copy in the HTML and the appearance payload showing our `var(--...)` overrides layered on the `dark` theme.
- Project dialogs & editor home — see `context/feature-specs/04-project-dialogs.md`. Added `types/project.ts` (`Project`, `ProjectRole`, `ProjectDialogKind`, and a `ProjectDialogIntent` discriminated union), `lib/slug.ts` (`slugify()` — NFKD-normalizes, strips diacritics, collapses non-alphanumerics to single dashes, caps at 60 chars; shared so the dialog preview and future server-side creation cannot drift), and `lib/mock-projects.ts` (five placeholder projects, three owned / two collaborator, slugs derived through `slugify`). `hooks/use-project-dialogs.ts` is the spec's dedicated hook: it owns which dialog is open, the name being edited, the derived `slugPreview`, `isSubmitting`, and `canSubmit`, and emits a `ProjectDialogIntent` to an optional `onSubmit` callback — the dialog stays open and shows loading until that settles, closes only on success, and refuses to close mid-submit. `hooks/use-mock-projects.ts` holds the mock list in component state and applies intents so the wired actions visibly work (UI state only — a reload restores the original list; no API calls, no persistence). UI: `components/editor/editor-home.tsx` (heading, description, `New Project` button with `Plus`; no cards, per spec), `components/editor/create-project-dialog.tsx` (name input + live `aria-live` slug preview), `rename-project-dialog.tsx` (prefilled input, current name in the description, `initialFocus` on the field with select-on-focus, Enter submits via native form submit, Save disabled while unchanged), `delete-project-dialog.tsx` (confirmation only, no input, `variant="destructive"` confirm), `project-dialogs.tsx` (mounts all three from one controller), and `project-list-item.tsx` (name + mono slug, with `Pencil`/`Trash2` actions gated on `project.role === "owner"`). `project-sidebar.tsx` now renders both project lists, keeps its empty states, wires create/rename/delete, adds a mobile-only backdrop scrim (`md:hidden`, tap to close), and marks the closed sidebar `inert` so its controls leave the tab order during the slide transition. Verified via `tsc --noEmit`, `eslint` (both clean), `next build`, a grep of the prerendered `.next/server/app/editor.html` (home copy, owned project names/slugs, and per-project `Rename …`/`Delete …` labels all present), a check that the generated CSS emits both `group-focus-within/project` and the `@media (hover:none)` fallback that keeps row actions visible on touch, and a compiled unit check of `slugify` across accented, punctuation-only, over-length, and multi-space inputs.

## In Progress

- None.

## Next Up

- Project persistence: Prisma `Project` model, `app/api` route handlers with ownership checks, and swapping `useMockProjects` for real data behind the existing `ProjectDialogIntent` seam. `lib/slug.ts` should be reused server-side so client preview and stored slug agree, and slug uniqueness (currently unenforced — two projects can preview the same slug) needs a rule.

## Open Questions

- `app/globals.css` now defines *both* token sets: the `ui-context.md` palette (`--bg-base`, `--text-primary`, `--accent-primary`, exposed as `bg-base`, `text-copy-primary`, `text-brand`, …) and the shadcn semantic layer (`--background`, `--card`, `--popover`, `--muted`, `--border`, …) mapped on top of it, so the earlier "these tokens do not exist" note is resolved. Two names still address the same color, though, and the shipped `components/ui/*` only speak the shadcn layer. Project dialogs were written against the shadcn names (`bg-card`, `text-muted-foreground`, `border-border/50`) to stay consistent with the existing navbar/sidebar. Worth deciding which layer is canonical for app-level components before the canvas work.
- `ui-context.md` puts modals at `rounded-3xl`, but the installed `components/ui/dialog.tsx` (a protected foundation file) hardcodes `rounded-xl` on the popup and `rounded-b-xl` on the footer. The dialogs use the primitive's radius rather than overriding it per-dialog, so every dialog stays visually identical. If `rounded-3xl` is the intent, change it once in the primitive.
- `context/feature-specs/03-auth.md` says to define public routes "using the existing sign-in and sign-up env vars," but `.env.local` had no such vars. Added `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` (Clerk's own standard var names, not invented ones) rather than blocking on this — flagging here in case that wasn't the intended resolution.

## Architecture Decisions

- `ProjectSidebar` is `position: fixed`, anchored `top-12` (matching the navbar's `h-12`) so it overlays the canvas below the navbar without pushing layout, per the "should not push page content" requirement.
- Auth route protection uses a protected-first `proxy.ts` strategy (block everything, allow-list `/`, `/sign-in(.*)`, `/sign-up(.*)`) rather than public-first, matching `architecture-context.md`'s "Only authenticated users can access protected routes" invariant and the spec's "Protect everything else by default."
- `/` owns its own redirect logic at the page level (Server Component + `auth()`) rather than relying on the proxy's default `auth.protect()` redirect, since the spec calls out `/`'s authenticated/unauthenticated behavior as a distinct requirement; `/` is in the proxy's public allow-list so this logic isn't shadowed.

- Project mutations flow through a single `ProjectDialogIntent` union (`create` / `rename` / `delete`) emitted by `useProjectDialogs`, rather than three separate callbacks. The editor currently hands it to `useMockProjects`; the persistence chapter swaps that one callback for an API call without touching the hook or any dialog.
- `slugify` lives in `lib/` rather than inside the create dialog, because `architecture-context.md` requires ownership/validation at the mutation boundary — the server will need to derive the identical slug from the same name.
- Sidebar row actions are inline hover/focus-revealed icon buttons instead of a dropdown menu: only two actions exist, it avoids a popup nested inside the sidebar overlay, and it needs no new `components/ui/*` file (protected per `ai-workflow-rules.md`). Tailwind v4 scopes `group-hover` behind `@media (hover:hover)`, so an explicit `@media (hover:none)` rule keeps the actions permanently visible on touch devices.
- Owner-only actions are gated inside `ProjectListItem` on `project.role === "owner"` rather than by which list rendered the row, so the rule holds wherever a project row is reused later.

- No headless browser tooling is installed, so the project dialogs were verified by build + prerendered-HTML grep + a compiled `slugify` check rather than by clicking through them. Dialog open/close, focus behavior, and the mobile scrim have not been exercised in a real browser.

## Session Notes

- No headless browser tooling (playwright/puppeteer/chromium) is installed in this container, so the editor chrome was verified via `tsc`, `eslint`, `next build`, and a curl-based HTML smoke test rather than a rendered screenshot. If visual verification is needed later, install a Chromium-based driver first. Same constraint applied to the auth feature — sign-in/sign-up visuals and the authenticated `UserButton` state were not visually verified in a browser.
